name: Promote Imported Configs

on:
  pull_request:
    types: [closed]
    branches: [ main ]
    paths:
      - 'importer_tmp_dir/**'

jobs:
  promote:
    if: github.event.pull_request.merged == true
    uses: G-Research/github-terraformer/.github/workflows/promote-imported-configs.yaml@gr-oss-1125-resolve-integration-id-to-source-name-v2
    with:
      commit_sha: ${{ github.event.pull_request.merge_commit_sha }}
      gcss_ref: gr-oss-1125-resolve-integration-id-to-source-name-v2
      pr_number: ${{ github.event.pull_request.number }}
      tfc_org: "GR-OSS"
    secrets:
      app_private_key: ${{ secrets.APP_PRIVATE_KEY }}
      tfc_token: ${{ secrets.TFC_TOKEN }}
