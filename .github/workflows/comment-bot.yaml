name: Post comment on PR

on:
  pull_request_target:

jobs:
  comment-bot:
    uses: G-Research/github-terraformer/.github/workflows/tf-apply.yaml@add-pr-comment-bot-reusable-workflow
    secrets:
      gh_token: ${{ secrets.GITHUB_TOKEN }}