name: "Drift Check"

on:
#  schedule:
#    - cron: '*/15 * * * *'
 workflow_dispatch:

jobs:
  drift-check:
    secrets:
      tfc_token: ${{ secrets.TFC_TOKEN }}
    uses: G-Research/github-terraformer/.github/workflows/drift-check.yaml@upgrade-terraform-terraform-provider-version
    with:
      gcss_ref: upgrade-terraform-terraform-provider-version
      tfc_org: "GR-OSS"