name: "Drift Check"

on:
#  schedule:
#    - cron: '*/15 * * * *'
 workflow_dispatch:

jobs:
  drift-check:
    secrets: inherit
    uses: gr-oss-developers/github-configuration-self-service/.github/workflows/drift-check.yaml@main
    with:
      gcss_ref: main