name: Bulk import repos pipeline

on:
  workflow_dispatch:

jobs:
  bulk-import-repos:
    uses: G-Research/github-terraformer/.github/workflows/bulk-import.yaml@make-integration-id-optional
    with:
      gcss_ref: make-integration-id-optional
    secrets:
      app_private_key: ${{ secrets.APP_PRIVATE_KEY }}