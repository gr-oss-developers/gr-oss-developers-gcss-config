name: Bulk import repos pipeline

on:
  workflow_dispatch:

jobs:
  bulk-import-repos:
    uses: G-Research/github-terraformer/.github/workflows/bulk-import.yaml@gr-oss-task-1119
    with:
      gcss_ref: gr-oss-task-1119
    secrets:
      app_private_key: ${{ secrets.APP_PRIVATE_KEY }}