name: Bulk import repos pipeline

on:
  workflow_dispatch:

jobs:
  bulk-import-repos:
    uses: G-Research/github-terraformer/.github/workflows/bulk-import.yaml@remove-unused-outputs
    with:
      gcss_ref: remove-unused-outputs
    secrets:
      app_private_key: ${{ secrets.APP_PRIVATE_KEY }}