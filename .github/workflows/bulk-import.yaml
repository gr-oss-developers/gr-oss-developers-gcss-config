name: Bulk import repos pipeline

on:
  workflow_dispatch:

jobs:
  bulk-import-repos:
    uses: G-Research/github-terraformer/.github/workflows/bulk-import.yaml@fix-check-run-text-overflow
    with:
      gcss_ref: fix-check-run-text-overflow
    secrets:
      app_private_key: ${{ secrets.APP_PRIVATE_KEY }}